import { HashConnect } from '../hashconnect';
import { AccountId } from "@hashgraph/sdk";
import { MessageTypes } from '../main';
export declare class HashConnectProviderSender {
    private readonly hashconnect;
    private readonly topicId;
    constructor(hashconnect: HashConnect, topicId: string);
    send(accountId: AccountId, message: Uint8Array, returnTransaction: boolean): Promise<MessageTypes.TransactionResponse>;
}
