export class HashConnectProviderSender {
    constructor(hashconnect, topicId) {
        this.hashconnect = hashconnect;
        this.topicId = topicId;
    }
    send(accountId, message, returnTransaction) {
        const transaction = {
            byteArray: message,
            metadata: {
                accountToSign: accountId.toString(),
                returnTransaction: returnTransaction,
            },
            topic: this.topicId,
        };
        return this.hashconnect.sendTransaction(this.topicId, transaction);
    }
}
//# sourceMappingURL=sender.js.map